package com.example.skybox_frontend.ui.login;

public class LoginModel {

    // Mock login logic
    public boolean login(String callsign, String password) {
        // Simulated check (replace with real auth later)
        // return "testuser".equals(callsign) && "password123".equals(password);
        return true;
    }
}
