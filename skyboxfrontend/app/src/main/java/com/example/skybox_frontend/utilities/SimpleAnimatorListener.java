package com.example.skybox_frontend.utilities;

import android.animation.Animator;
import android.animation.AnimatorListenerAdapter;

public abstract class SimpleAnimatorListener extends AnimatorListenerAdapter {
    // Extend this class and override only what you need (e.g., onAnimationEnd)
}
